
interface PageProps {
  params: {
    id: string;
    token:string;
  };
}

export default async function LoginPage({ params }: PageProps) {
  // Tu código actual
  return (
    <div>
      {/* Contenido */}
    </div>
  )
} 